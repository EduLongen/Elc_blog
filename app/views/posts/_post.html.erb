<div id="<%= dom_id post %>">

  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <h6><%= pluralize(post.views, "view") %></h6>

  <p>
    <strong>Content:</strong>
    <%= post.content %>
  </p>

</div>
